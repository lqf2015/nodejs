参考链接 nodebeginner.org/index-zh-cn.html

 ls -lah 是显示当前目录下的文件
 > /home/c.txt 表示 输出到这个文件中

一、需求
  1,用户可以通过浏览器使用我们的应用
  2,当用户请求http://domain/start时,可以看到一个欢迎页面,页面上有一个文件上传的表单
  3,用户可以选择一个图片并提交表单,随后文件将被上传到http://domain/upload,改也献上传后会把图片现在在页面上。


二、分析

  1,我们需要提供web页面,因此需要一个http服务器
  2,对于不同的请求,根据请求的url,我们的服务器要给与不同的响应,
    因此我们需要一个路由,用于把请求对应到请求处理程序(request handler)
  3,当请求被服务器接收并通过路由传递之后,需要可以对其进行处理,因此我们需要最终的请求处理程序
  4,路由还应该能处理post数据,并且把数据封装成更友好的格式传递给请求处理入程序,因此需要请求数据处理功能
  5,我们不仅仅要处理url对应的赢球,还要把内容显示出来,这意味着我们需要一些视图逻辑请求处理程序使用,一遍将内容发送给用户的浏览器
  6,最后,用户需要上传图片,所以我们需要上传处理功能来处理这方面的细节。


三、